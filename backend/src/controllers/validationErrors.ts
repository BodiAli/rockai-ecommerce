const validationErrors = {
  emptyErr: "can not be empty.",
  maxLengthErr255: "can not exceed 255 characters.",
  maxLengthErr100: "can not exceed 100 characters.",
  priceError: "Price must be a valid price.",
};

export default validationErrors;
